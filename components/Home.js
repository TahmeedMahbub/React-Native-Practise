import {View, Text, StyleSheet, StatusBar, Button} from 'react-native';
import App from '../App';

export const Home = () => {
    return (
        <View>
            <Text style={{ fontSize: 20, margin: 5, padding: 5 }}>Home Screen</Text>
        </View>
    )
}